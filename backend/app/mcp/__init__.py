"""MCP server — tool surface exposing the same use-cases as the HTTP API.

A sibling adapter of ``app.api``: both drive ``app.services`` and neither may
import the other (enforced by the import-linter contracts in
``backend/pyproject.toml``).

Filled in by a later work package.
"""
