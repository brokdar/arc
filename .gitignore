# === Python ===
__pycache__/
*.py[codz]
*$py.class
*.so
build/
dist/
wheels/
*.egg-info/
*.egg

# Test / coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
.pytest_cache/
.hypothesis/

# Environments
.env
.env.local
.envrc
.venv
venv/

# Tooling caches
.ruff_cache/
.pyrefly/
.mypy_cache/
.schemathesis/

# === Node / Next.js ===
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
coverage/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# === Editors / OS ===
.DS_Store
.idea/
*.log

# === Project ===
logs/
# The runtime data tree (inbox/, originals/, streams/, quarantine/) at the
# repository root. Anchored: an unanchored `data/` also swallows
# `backend/tests/data/`, where WP-4's committed golden FIT files live.
/data/
.data-dev/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
**/agent-memory/

# Local docs stay untracked (see PR #6), with named exceptions that are
# shared instruments rather than working notes. `docs/*` instead of `docs/`
# because git will not re-include a file inside an ignored directory.
docs/*
!docs/agent-eval.md
